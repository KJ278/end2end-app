name: secure_private_messaging
publish_to: none
description: Invite-only end-to-end encrypted private messaging client.
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cryptography: ^2.7.0
  flutter_secure_storage: ^9.2.2
  get_it: ^7.7.0
  riverpod: ^2.5.1
  go_router: ^14.2.0
  dio: ^5.5.0
  sqflite_sqlcipher: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0
